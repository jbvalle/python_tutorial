# Use dictionaries when you want to store data in a key-value
# format, e.g., storing user information with their IDs as keys.

my_dict = {"name": "jafer", "age": 25}

print(my_dict["name"])
print(my_dict["age"])
